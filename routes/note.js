var db = require('../models')

/*
 * GET all alerts
 */
// exports.getAll = function(request, response){
//   db.Alert.findAll({
//     include: [ db.Note ]
//   }).success(function(alerts) {
//     return alerts;
//   })
  
// };